version: 0.2

phases:
  install:
    runtime-versions:
      docker: 20

  build:
    commands:
      - echo "Build started"
      - docker build -t myapp .
      - docker run myapp

artifacts:
  files:
    - '**/*'